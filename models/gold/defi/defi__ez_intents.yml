version: 2

models:
  - name: defi__ez_intents
    description: "{{ doc('defi__ez_intents') }}"
    # Note: This is now a view sourcing from intents__ez_transactions
    # Tests are performed on the underlying intents__ tables

    columns:
      - name: BLOCK_ID
        description: "{{ doc('block_id') }}"

      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp') }}"

      - name: TX_HASH
        description: "{{ doc('tx_hash') }}"

      - name: RECEIPT_ID
        description: "{{ doc('receipt_id') }}"

      - name: PREDECESSOR_ID
        description: "{{ doc('predecessor_id') }}"

      - name: RECEIVER_ID
        description: "{{ doc('receiver_id') }}"

      - name: MEMO
        description: "{{ doc('memo') }}"

      - name: AMOUNT_RAW
        description: "{{ doc('amount_raw') }}"

      - name: TOKEN_ID
        description: "{{ doc('token_id') }}"

      - name: REFERRAL
        description: "{{ doc('referral') }}"

      - name: FEES_COLLECTED_RAW
        description: "{{ doc('fees_collected_raw') }}"

      - name: FEE_TOKEN
        description: "{{ doc('fee_token') }}"

      - name: FEE_AMOUNT_ADJ
        description: "{{ doc('fee_amount_adj') }}"

      - name: FEE_AMOUNT_USD
        description: "{{ doc('fee_amount_usd') }}"

      - name: DIP4_VERSION
        description: "{{ doc('dip4_version') }}"

      - name: GAS_BURNT
        description: "{{ doc('gas_burnt') }}"

      - name: RECEIPT_SUCCEEDED
        description: "{{ doc('receipt_succeeded') }}"

      - name: AMOUNT_INDEX
        description: "{{ doc('amount_index') }}"

      - name: LOG_EVENT
        description: "{{ doc('log_event') }}"

      - name: LOG_INDEX
        description: "{{ doc('log_index') }}"

      - name: LOG_EVENT_INDEX
        description: "{{ doc('log_event_index') }}"

      - name: OWNER_ID
        description: "{{ doc('owner_id_intent') }}"

      - name: OLD_OWNER_ID
        description: "{{ doc('old_owner_id') }}"

      - name: NEW_OWNER_ID
        description: "{{ doc('new_owner_id') }}"

      - name: BLOCKCHAIN
        description: "{{ doc('blockchain') }}"

      - name: CONTRACT_ADDRESS
        description: "{{ doc('contract_address') }}"

      - name: IS_NATIVE
        description: "{{ doc('is_native') }}"

      - name: SYMBOL
        description: "{{ doc('symbol') }}"

      - name: DECIMALS
        description: "{{ doc('decimals') }}"

      - name: AMOUNT_ADJ
        description: "{{ doc('amount_adj') }}"

      - name: PRICE
        description: "{{ doc('price_usd') }}"

      - name: AMOUNT_USD
        description: "{{ doc('amount_usd') }}"

      - name: TOKEN_IS_VERIFIED
        description: "{{ doc('token_is_verified') }}"

      - name: EZ_INTENTS_ID
        description: "{{ doc('id') }}"

      - name: INSERTED_TIMESTAMP
        description: "{{doc('inserted_timestamp')}}"

      - name: MODIFIED_TIMESTAMP
        description: "{{doc('modified_timestamp')}}"

      - name: _INVOCATION_ID
        description: "{{ doc('invocation_id') }}"
