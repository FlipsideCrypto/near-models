
version: 2

models:
  - name: metrics_daily_transactions
    description: "Daily number of near transactions."

    columns:
      - name: date
        description: "{{ doc('date')}}"
        tests:
          - not_null

      - name: daily_transactions
        description: "{{ doc('daily_transactions')}}"
        tests:
          - not_null
